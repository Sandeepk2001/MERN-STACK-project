{
  "name": "my-jenkins-app",
  "version": "1.0.0",
  "description": "A simple test for Jenkins",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
